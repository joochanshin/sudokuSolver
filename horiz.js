function horiz1(num, grid){	
	let y = 0;
    for(let x = 0; x < 9; x++){
        //console.log(num, grid[(x) + (y)*9])
        if(num == grid[(x) + (y)*9]){
            //console.log("In horizIf " + grid[(x) + (y)*9])
            return true;
                //console.log("X: " + x + " Y: " + y + " Value: " + grid[(x) + (y)*9]);
        }    
    }  
}

function horiz2(num, grid){	
	let y = 1;
    for(let x = 0; x < 9; x++){
        //console.log(num, grid[(x) + (y)*9])
        if(num == grid[(x) + (y)*9]){
            //console.log("In horizIf " + num + " " + grid[(x) + (y)*9])
            return true;
                //console.log("X: " + x + " Y: " + y + " Value: " + grid[(x) + (y)*9]);
        }    
    }  
}

function horiz3(num, grid){	
	let y = 2;
    for(let x = 0; x < 9; x++){
        //console.log(num, grid[(x) + (y)*9])
        if(num == grid[(x) + (y)*9]){
            //console.log("In horizIf " + grid[(x) + (y)*9])
            return true;
                //console.log("X: " + x + " Y: " + y + " Value: " + grid[(x) + (y)*9]);
        }    
    }  
}

function horiz4(num, grid){	
	let y = 3;
    for(let x = 0; x < 9; x++){
        //console.log(num, grid[(x) + (y)*9])
        if(num == grid[(x) + (y)*9]){
            //console.log("In horizIf " + grid[(x) + (y)*9])
            return true;
                //console.log("X: " + x + " Y: " + y + " Value: " + grid[(x) + (y)*9]);
        }    
    }  
}

function horiz5(num, grid){	
	let y = 4;
    for(let x = 0; x < 9; x++){
        //console.log(num, grid[(x) + (y)*9])
        if(num == grid[(x) + (y)*9]){
           // console.log("In horizIf " + grid[(x) + (y)*9])
            return true;
                //console.log("X: " + x + " Y: " + y + " Value: " + grid[(x) + (y)*9]);
        }    
    }  
}

function horiz6(num, grid){	
	let y = 5;
    for(let x = 0; x < 9; x++){
        //console.log(num, grid[(x) + (y)*9])
        if(num == grid[(x) + (y)*9]){
            //console.log("In horizIf " + grid[(x) + (y)*9])
            return true;
                //console.log("X: " + x + " Y: " + y + " Value: " + grid[(x) + (y)*9]);
        }    
    }  
}

function horiz7(num, grid){	
	let y = 6;
    for(let x = 0; x < 9; x++){
        //console.log(num, grid[(x) + (y)*9])
        if(num == grid[(x) + (y)*9]){
            //console.log("In horizIf " + grid[(x) + (y)*9])
            return true;
                //console.log("X: " + x + " Y: " + y + " Value: " + grid[(x) + (y)*9]);
        }    
    }  
}

function horiz8(num, grid){	
	let y = 7;
    for(let x = 0; x < 9; x++){
        //console.log(num, grid[(x) + (y)*9])
        if(num == grid[(x) + (y)*9]){
            //console.log("In horizIf " + grid[(x) + (y)*9])
            return true;
                //console.log("X: " + x + " Y: " + y + " Value: " + grid[(x) + (y)*9]);
        }    
    }  
}

function horiz9(num, grid){	
	let y = 8;
    for(let x = 0; x < 9; x++){
        //console.log(num, grid[(x) + (y)*9])
        if(num == grid[(x) + (y)*9]){
            //console.log("In horizIf " + grid[(x) + (y)*9])
            return true;
                //console.log("X: " + x + " Y: " + y + " Value: " + grid[(x) + (y)*9]);
        }    
    }  
}